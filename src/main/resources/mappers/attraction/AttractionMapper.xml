<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.nayeogi.attraction.model.dao.AttractionDao">

	<select id="findByTitle"
		parameterType="string"
		resultType="com.ssafy.nayeogi.attraction.model.dto.AttractionResponse">
		SELECT
			id,
			content_id,
			title,
			content_type_id,
			area_code,
			si_gun_gu_code,
			first_image1,
			first_image2,
			map_level,
			latitude,
			longitude,
			tel,
			addr1,
			addr2,
			homepage,
			overview
		FROM attractions
		WHERE title LIKE CONCAT('%', #{title}, '%')
		ORDER BY title ASC, id ASC
	</select>

</mapper>
